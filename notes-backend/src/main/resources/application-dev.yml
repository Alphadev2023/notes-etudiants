spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notes_db
    username: postgres
    password: postgres

  jpa:
    show-sql: true

  mail:
    host: smtp.gmail.com
    username: dev@notes.com
    password: dev_password

logging:
  level:
    com.gestion: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG